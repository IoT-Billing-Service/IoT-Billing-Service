[package]
name = "iot-billing-escrow"
version = "1.0.0"
authors = ["IoT Billing Service Developers <onojajerome04@gmail.com>"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.0.0"

[dev-dependencies]
soroban-sdk = { version = "20.0.0", features = ["testutils"] }
